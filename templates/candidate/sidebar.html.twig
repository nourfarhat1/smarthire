<!-- Candidate Sidebar Navigation -->
<div class="position-sticky pt-3">
    <!-- Candidate Menu (Green header #28a745) -->
    <div class="candidate-menu mb-4">
        <div class="role-header text-center py-2 mb-3 rounded" style="background-color: #28a745;">
            <small class="text-white fw-bold">CANDIDATE</small>
        </div>
        <ul class="nav flex-column">
            <li class="nav-item">
                <a class="nav-link text-white {{ app.request.get('_route') == 'app_candidate_dashboard' ? 'active' : '' }}" href="{{ path('app_candidate_dashboard') }}">
                    <i class="bi bi-speedometer2 me-2"></i> Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white {{ app.request.get('_route') starts with 'app_candidate_profile' ? 'active' : '' }}" href="{{ path('app_candidate_profile') }}">
                    <i class="bi bi-person-circle me-2"></i> My Profile
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white {{ app.request.get('_route') == 'app_candidate_quizzes' ? 'active' : '' }}" href="{{ path('app_candidate_quizzes') }}">
                    <i class="bi bi-question-circle me-2"></i> Quizzes
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white {{ app.request.get('_route') starts with 'app_candidate_events' ? 'active' : '' }}" href="{{ path('app_candidate_events') }}">
                    <i class="bi bi-calendar-event me-2"></i> My Events
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white {{ app.request.get('_route') == 'app_candidate_applications' ? 'active' : '' }}" href="{{ path('app_candidate_applications') }}">
                    <i class="bi bi-briefcase me-2"></i> Job Applications
                </a>
            </li>
        </ul>
        
        <!-- Profile & Logout Section -->
        <div class="mt-4 pt-3 border-top border-light">
            <ul class="nav flex-column">
                <li class="nav-item">
                    <a class="nav-link text-white {{ app.request.get('_route') starts with 'app_candidate_profile' ? 'active' : '' }}" href="{{ path('app_candidate_profile') }}">
                        <i class="bi bi-person-circle me-2"></i> My Profile
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-danger" href="{{ path('app_logout') }}">
                        <i class="bi bi-box-arrow-right me-2"></i> Logout
                    </a>
                </li>
            </ul>
        </div>
    </div>
</div>

<style>
.sidebar {
    position: fixed;
    top: 0;
    bottom: 0;
    left: 0;
    z-index: 100;
    padding: 48px 0 0;
    box-shadow: inset -1px 0 0 rgba(0, 0, 0, .1);
}

.candidate-menu {
    margin-bottom: 1rem;
}

.role-header {
    font-size: .75rem;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.sidebar .nav-link {
    font-weight: 500;
    color: #333;
    padding: 0.75rem 1rem;
    border-radius: 0.375rem;
    margin: 0.125rem 0.75rem;
    transition: all 0.3s ease;
}

.sidebar .nav-link:hover {
    color: #fff;
    background-color: rgba(40, 167, 69, 0.1);
    transform: translateX(5px);
}

.sidebar .nav-link.active {
    color: #fff;
    background-color: rgba(40, 167, 69, 0.2);
    font-weight: 600;
    transform: translateX(5px);
}

.sidebar .nav-link.disabled {
    color: #6c757d;
    background-color: transparent;
    cursor: not-allowed;
}

.sidebar .nav-link.disabled:hover {
    color: #6c757d;
    background-color: transparent;
    transform: none;
}

.sidebar .nav-link i {
    width: 16px;
    text-align: center;
}

@media (max-width: 767.98px) {
    .sidebar {
        position: relative;
        height: auto;
        padding: 0;
    }
}
</style>
